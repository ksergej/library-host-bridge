all:
  children:
    zos_xplore:
      hosts:
        zos1:
          # Tracked placeholder only. Real host values come from:
          # - GitHub Actions secrets for host-ci
          # - local override vars for manual runs
          ansible_host: 192.0.2.10
